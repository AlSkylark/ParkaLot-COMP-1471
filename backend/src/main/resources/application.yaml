spring:
  application:
    name: api
  datasource:
    url: jdbc:postgresql://localhost:5432/dev_parkalot
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    
server:
  port: 8080
  
